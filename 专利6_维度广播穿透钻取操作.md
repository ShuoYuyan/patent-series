# 发明专利文档

## 发明名称: 多维数据维度广播与穿透钻取操作方法

### 技术领域

本发明涉及数据操作技术领域，具体涉及一种多维数据的维度广播操作和穿透钻取操作方法，实现高效批量数据处理和多层级数据查询。

### 背景技术

传统数据操作方法存在以下局限：

1. **批量操作困难**：传统方式需要循环遍历每个数据点逐一操作，效率低、代码冗长。
2. **条件操作复杂**：按条件筛选并操作特定数据点需要编写复杂的过滤逻辑。
3. **多层级查询困难**：从汇总数据穿透到明细数据需要多次查询，效率低。
4. **聚合分析低效**：多维数据的分组聚合需要预定义聚合规则，灵活性差。
5. **数据投影繁琐**：提取特定维度的数据需要编写复杂的提取逻辑。

### 发明内容

本发明提出多维数据的维度广播操作和穿透钻取方法，实现高效批量数据处理和多层级数据查询。

#### 核心创新

1. **维度广播**：将单一操作广播到N维数据的所有维度元素，一条语句操作数千个数据点。
2. **条件广播**：在广播基础上，按条件筛选特定数据点进行操作。
3. **穿透钻取**：从汇总维度逐层穿透到明细维度，实现多层级数据查询。
4. **维度投影**：从N维数据中提取指定维度的子集，形成新的数据视图。
5. **维度透视**：按多维度分组聚合，支持灵活的报表分析。

#### 技术方案

```
维度广播:
  broadcast 路径 = 值
  → 将值广播到路径匹配的所有数据点

条件广播:
  broadcast 路径[@条件] = 值
  → 将值广播到满足条件的数据点

维度投影:
  project 路径 as 别名
  → 提取指定维度的数据子集

维度过滤:
  filter 路径 where 条件
  → 过滤满足条件的数据点

维度透视:
  pivot 路径 rows=[维度] cols=[维度] vals=[维度] agg=聚合函数
  → 按多维度分组聚合

维度上卷:
  roll_up 路径 count 维度
  → 汇总维度数据
```

#### 维度广播操作

```tcl
# 将所有页面所有色版所有元素的填充色设为黑色
broadcast pfd.page[*].colorplate[*].element[*].fill = "#000000"

# 将所有对象的运动类型设为正弦
broadcast objects[*].relation = sine(amplitude=50)

# 条件广播：只修改红色元素的描边
broadcast pfd.page[*].colorplate[*].element[@.fill=="#FF0000"].stroke = "#000000"
```

#### 穿透钻取操作

```tcl
# 1. 维度投影：提取所有元素的位置信息
project pfd.page[*].colorplate[*].element[*].ndData.data[0] as positions
# 返回: [{x=10, y=10}, {x=48.5, y=10}, ...]

# 2. 维度过滤：只选择第一行的元素
filter pfd.page[*].colorplate[*].element[*] where metadata.position.y < 50

# 3. 维度上卷：统计每页的元素数量
roll_up pfd.page[*] count elements

# 4. 维度透视：按色版和页面分组统计
pivot pfd.page[*].colorplate[*] rows=[colorplate] cols=[page] vals=[count] agg=SUM
```

#### 执行效率

| 操作 | 传统方式 | 本发明 | 优化手段 |
|------|----------|--------|----------|
| 单点读 | O(1) | O(1) | 路径索引 |
| 批量广播 | O(n)串行 | O(n)并行 | 维度广播内部并行化 |
| 条件过滤 | O(n)遍历 | O(n)评估 | 谓词下推+索引 |
| 透视分析 | O(n)分组 | O(n)聚合 | 维度索引+缓存 |
| 递归下降 | O(树大小) | O(树大小) | PathIndex+广度优先 |

### 有益效果

1. **高效批量**：维度广播内部并行化，一条语句操作数千个数据点，效率提升100-10000倍。
2. **精确条件**：条件广播支持谓词过滤，精确操作特定数据点。
3. **多层穿透**：穿透钻取支持从汇总到明细的逐层查询，灵活性极高。
4. **灵活聚合**：维度透视支持多维度分组聚合，报表分析能力强。
5. **统一语法**：广播、投影、过滤、透视使用统一语法，学习成本低。
6. **跨领域通用**：同一套操作方法适用于所有领域。

### 附图说明

图1：维度广播操作示意图
图2：穿透钻取操作流程图
图3：维度透视示意图
图4：执行效率对比图

### 具体实施方式

**实施例1：印刷批量操作**

```tcl
# 批量设置所有页面的出血
broadcast pfd.page[*].bleed = 3mm

# 批量设置所有专色的启用状态
broadcast pfd.page[*].colorplate[4].enabled = true

# 条件批量：只设置CMYK版的颜色
broadcast pfd.page[*].colorplate[0..3].element[*].fill = "#000000"
```

**实施例2：动画批量操作**

```tcl
# 批量设置所有对象的颜色
broadcast objects[*].color = hsl(${index}*36, 70%, 50%)

# 批量设置所有对象的运动
broadcast objects[*].relation = sine(amplitude=50, frequency=1)

# 条件批量：只修改大尺寸对象
broadcast objects[@.size>100].strokeWidth = 2
```

**实施例3：数据穿透分析**

```tcl
# 从汇总数据穿透到明细
project pfd.page[*].colorplate[*].element[*].ndData.data[0] as positions
filter positions where position.x > 100
roll_up pfd.page[*] count elements
pivot pfd.page[*].colorplate[*] rows=[colorplate] cols=[page] vals=[count] agg=SUM
```

---

## 补充：TD+ND+DD三层穿透钻取（基于`透明统一路径_DD穿透设计方案.md`）

### 补充6.1：三层穿透钻取定义

```
穿透钻取:
  TD穿透: 从汇总文档穿透到明细文档
    get shucang.document[0] → 定位单个文档
  
  ND穿透: 从文档穿透到ND多维数据
    get shucang.document[0].field[3] → 定位ND cell
  
  DD穿透: 从ND cell穿透到JSON内部字段
    get shucang.document[0].field[3].user.name → 定位JSON字段
  
  三层穿透: TD → ND → DD 串联
    get shucang.document[*].field[3].user.addr.city
```

### 补充6.2：DD层穿透操作扩展

| 操作 | 语法 | 说明 |
|------|------|------|
| 字段访问 | `.fieldName` | 进入JSON对象字段 |
| 数组索引 | `[index]` | 进入JSON数组索引 |
| 通配遍历 | `[*]` | 遍历JSON数组所有元素 |
| 数组切片 | `[start:end]` | JSON数组切片 |
| 谓词过滤 | `[@predicate]` | JSON数组/对象谓词过滤 |
| 递归下降 | `..fieldName` | 任意深度搜索字段 |

### 补充6.3：ND谓词+DD穿透组合

```tcl
# ND谓词筛选 + DD穿透 (原E.4限制放宽)
get shucang.document[*].field[3].user.name
# 语义: 筛选field[3]为Object的ND cell → DD穿透进入user.name

# ND谓词+DD谓词组合
get shucang.document[*].data[@.type == "user"].name
# 语义: ND谓词筛选type=user的cell → DD穿透获取name
```

### 补充6.4：穿透钻取性能优化

| 优化手段 | 适用层级 | 效果 |
|----------|----------|------|
| 路径缓存 | TD+ND | O(1)重复查询 |
| DD字段倒排索引 | DD | 跳过全表扫描 |
| JSON结构共享模板 | DD | 5-20倍存储压缩 |
| 向量化批量穿透 | DD | 批量操作效率提升100倍 |
| JIT专用化执行计划 | 全层 | 运行时优化 |

### 补充6.5：跨领域穿透钻取示例

| 领域 | 三层穿透示例 |
|------|--------------|
| 办公文档 | `get wenshu.doc[*].paragraph[3].style.font` |
| 印刷设计 | `get pfd.page[*].colorplate[0].element[5].attribute.trapping` |
| 动画创作 | `get objects[*].relation.sine.amplitude` |
| 印刷标准 | `get pfd.page[*].colorplate[4].element[*].attribute.overrides` |

---

## 许可与授权

**版权归属**：所有理论和基于理论体系发明的产品发明专利、著作权归四川龙映科技有限公司陈钦所有。

**授权条款**：本专利文档采用 **GNU AFFERO GENERAL PUBLIC LICENSE v3.0（AGPL-3.0）** 协议开源，并附加以下条款：

1. **引用规范**：任何人无偿使用、引用必须说明出处，包括发明人姓名、单位及原始文档链接。
2. **改进回馈**：改进后请回馈仓库，基于本专利文档的改进、衍生作品或增强版本，必须回馈至本仓库，不得私有化。
3. **非商用无偿**：非盈利用途可无偿使用，包括个人学习、学术研究、科研项目、开源社区贡献等。
4. **商用授权**：商用需获得授权付费使用。商业用途需获得书面授权并支付授权费用。商业用途包括但不限于：将本技术方案用于商业产品、商业服务、商业系统、商业咨询等。
5. **法律保护**：本产品受国际法的保护，侵权必究。
6. **禁止剽窃**：禁止剽窃本专利文档的内容并据为己有，禁止删除或篡改原始版权信息。

**发明人**: 陈钦 (cq800229@qq.com)  
**单位**: 四川龙映科技有限公司  
**申请日期**: 2026-09-14
