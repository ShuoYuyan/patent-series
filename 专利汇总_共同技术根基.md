# 发明专利汇总文档

## 共同技术根基：多维数据统一计算范式

### 贡献声明

本系列专利无偿用于任何非盈利用途。禁止商用、禁止剽窃或据为己有。商业使用需获得商业授权费。

### 背景

以下6项专利是从用户所有原创项目（UO/OfficeProject、DM/DrawMaster、AL/AnimationLanguage、PFD/PrintFormatDocument）中抽离出的共同技术根基。这4个项目虽然面向不同领域（办公文档、印刷设计、动画创作、打印格式），但共享同一套技术范式。本系列专利将这一范式从具体项目中抽离，形成独立可验证的发明。

### 项目与技术根基映射

| 项目 | 领域 | 使用的核心技术根基 |
|------|------|-------------------|
| UO (OfficeProject) | 数据→文档 | NDData + TCL + 树架构 + 双向对等 + Agent优先 + 维度广播 |
| DM (DrawMaster) | 数据→设计 | NDData + TQL + 树架构 + 双向对等 + Agent优先 + 维度广播 |
| AL (AnimationLanguage) | 数据→动画 | NDData + TQL + 树架构 + 双向对等 + Agent优先 + 维度广播 |
| PFD (PrintFormatDocument) | 设计→印刷 | NDData + TCL + 树架构 + 双向对等 + Agent优先 + 维度广播 |

### 统一范式公式

```
颠覆式创新 = NDData + TCL/TQL + Agent优先 + 不兼容incumbent + 从零设计
```

这一范式已在4个项目中独立验证：
1. UO：颠覆MS Office（TCL=ND+TD）
2. DM：颠覆PS/AI/ACAD（TQL=ND+TD）
3. AL：颠覆AE/Premiere（TQL=ND+TD+数学关系）
4. PFD：颠覆PDF（基于TDF原则，开放全球共享，不兼容PDF）

---

## 专利列表

### 专利1: N维数据管理系统及方法

- **文件**: `专利1_NDData_N维数据管理系统.md`
- **核心贡献**: 提出N维数据模型（NDData），统一表示任意领域的数据
- **关键创新**: N维数据模型、维度语义化、零拷贝操作、内存映射支持
- **DD穿透补充**: ND Cell复合类型扩展(DtypeVariant/Object/Array/Ref)、ND Cell内嵌TD树节点零拷贝绑定、JSON结构共享模板
- **跨项目验证**: UO(文档数据)、DM(设计数据)、AL(动画数据)、PFD(印刷数据)均使用

### 专利2: 统一配置查询语言(TCL/TQL)及数据操作方法

- **文件**: `专利2_TCL_TQL统一配置查询语言.md`
- **核心贡献**: 提出统一路径表达式语言，实现对N维数据的统一操作
- **关键创新**: 路径表达式统一、操作类型统一、维度感知、批量操作、穿透钻取
- **DD穿透补充**: DD路径段语法(.field/[index]/[*]/[start:end]/[@predicate]/..field)、三阶段解析流水线、ND谓词后允许DD段、上下文敏感谓词、DD层广播写入语义
- **跨项目验证**: UO(TCL)、DM(TQL)、AL(TQL)、PFD(TCL)均使用

### 专利3: 基于树架构与路径索引的数据存储查询系统

- **文件**: `专利3_树架构路径索引数据存储查询.md`
- **核心贡献**: 提出按需增长的树架构与O(1)路径索引
- **关键创新**: 树架构、按需增长、路径索引、节点自包含、维度坐标融合
- **DD穿透补充**: ND Cell与TD树节点零拷贝绑定、JSON结构共享模板、DD层倒排索引
- **跨项目验证**: UO(TDF树)、DM(TDF树)、AL(TDF树)、PFD(TDF树)均使用

### 专利4: 数据描述与操作双向对等架构

- **文件**: `专利4_双向对等架构.md`
- **核心贡献**: 提出数据描述层(NDData)与数据操作层(TCL/TQL)的双向对等
- **关键创新**: 双向对等、统一抽象、确定性映射、正交性
- **DD穿透补充**: TD+ND+DD三层双向对等、统一抽象扩展、正交性扩展
- **跨项目验证**: UO、DM、AL、PFD均遵循双向对等原则

### 专利5: Agent优先的多维数据自动化系统架构

- **文件**: `专利5_Agent优先架构.md`
- **核心贡献**: 提出从设计之初就为AI Agent服务的系统架构
- **关键创新**: Agent优先设计、自然语言接口、精确路径控制、批量操作原生支持、可组合操作、确定性语义
- **DD穿透补充**: Agent穿透JSON文档能力、Agent自然语言→DD穿透、工作流扩展、操作精度从文档级提升到字段级
- **跨项目验证**: UO、DM、AL、PFD均以Agent优先为设计原则

### 专利6: 多维数据维度广播与穿透钻取操作方法

- **文件**: `专利6_维度广播穿透钻取操作.md`
- **核心贡献**: 提出维度广播和穿透钻取操作方法，实现高效批量数据处理
- **关键创新**: 维度广播、条件广播、维度投影、维度过滤、维度透视、维度上卷
- **DD穿透补充**: TD+ND+DD三层穿透钻取、DD层穿透操作扩展、ND谓词+DD穿透组合、性能优化(倒排索引/结构共享/向量化)
- **跨项目验证**: UO、DM、AL、PFD均使用维度广播和穿透钻取

---

## TD+ND+DD三层融合：统一范式的最新演进

基于`透明统一路径_DD穿透设计方案.md`的设计，原始TD+ND两层融合扩展为TD+ND+DD三层融合：

```
原始范式: TD + ND = TCL
演进范式: TD + ND + DD = TCL (扩展)

TD层: 文档/对象/数组的层级导航
ND层: 多维数据的坐标定位与操作
DD层: JSON文档内部字段的穿透导航

JSON文档 = TD树(对象/数组节点) + ND叶子(string/number/bool/null)
TCL = TD + ND + DD
```

### 验证状态

| 专利 | UO验证 | DM验证 | AL验证 | PFD验证 | DD穿透补充 |
|------|--------|--------|--------|---------|------------|
| 专利1: NDData | ✅ | ✅ | ✅ | ✅ | ✅ 复合类型+零拷贝 |
| 专利2: TCL/TQL | ✅ | ✅ | ✅ | ✅ | ✅ DD路径+三阶段 |
| 专利3: 树架构 | ✅ | ✅ | ✅ | ✅ | ✅ 零拷贝+模板 |
| 专利4: 双向对等 | ✅ | ✅ | ✅ | ✅ | ✅ 三层对等 |
| 专利5: Agent优先 | ✅ | ✅ | ✅ | ✅ | ✅ DD穿透能力 |
| 专利6: 维度操作 | ✅ | ✅ | ✅ | ✅ | ✅ 三层穿透 |

---

## 技术根基依赖关系（更新）

```
专利1 (NDData) ──基础数据模型──→ 专利2 (TCL/TQL)
专利3 (树架构) ──存储引擎──────→ 专利2 (TCL/TQL)
专利4 (双向对等) ←── 专利1+专利2 ──→ 专利6 (维度操作)
专利5 (Agent优先) ←── 全部专利 ──→ 全部专利

DD穿透扩展:
专利1 ──复合类型──→ 专利2 (DD路径段)
专利3 ──零拷贝──→ 专利2 (DD解析)
专利6 ──三层穿透──→ 专利4 (三层对等)
```

---

## 许可与授权

**版权归属**：所有理论和基于理论体系发明的产品发明专利、著作权归四川龙映科技有限公司陈钦所有。

**授权条款**：本系列文档采用 **GNU AFFERO GENERAL PUBLIC LICENSE v3.0（AGPL-3.0）** 协议开源，并附加以下条款：

1. **引用规范**：任何人无偿使用、引用必须说明出处，包括发明人姓名、单位及原始文档链接。
2. **改进回馈**：改进后请回馈仓库，基于本系列文档的改进、衍生作品或增强版本，必须回馈至本仓库，不得私有化。
3. **非商用无偿**：非盈利用途可无偿使用，包括个人学习、学术研究、科研项目、开源社区贡献等。
4. **商用授权**：商用需获得授权付费使用。商业用途需获得书面授权并支付授权费用。商业用途包括但不限于：将本技术方案用于商业产品、商业服务、商业系统、商业咨询等。
5. **法律保护**：本产品受国际法的保护，侵权必究。
6. **禁止剽窃**：禁止剽窃本系列文档的内容并据为己有，禁止删除或篡改原始版权信息。

**发明人**: 陈钦 (cq800229@qq.com)  
**单位**: 四川龙映科技有限公司  
**申请日期**: 2026-09-14
