# 发明专利文档

## 发明名称: 基于树架构与路径索引的数据存储查询系统

### 技术领域

本发明涉及数据存储与查询技术领域，具体涉及一种基于树架构与路径索引的数据存储查询系统，适用于多领域统一数据的按需增长存储和O(1)路径查询。

### 背景技术

传统数据存储系统存在以下局限：

1. **固定结构**：关系型数据库需要预定义表结构，无法按需增长；文档数据库(MongoDB等)虽然支持动态结构，但查询效率随数据量下降。
2. **查询效率低**：关系型数据库的JOIN操作复杂度随数据规模增长；文档数据库的全表扫描效率低。
3. **空间浪费**：传统格式包含固定结构，即使不使用也占用空间。
4. **路径查询困难**：树状数据（如JSON/XML）的路径查询需要遍历，复杂度O(n)。
5. **多格式碎片化**：不同领域使用不同数据格式（JSON/XML/YAML/二进制），互不兼容。

### 发明内容

本发明提出一种基于树架构与路径索引的数据存储查询系统，实现按需增长的空间占用和O(1)路径查询。

#### 核心创新

1. **树架构**：数据以树结构组织，初始状态只有根节点（零空间占用），按需添加分支（按需增长）。
2. **路径索引**：为每个节点建立路径到节点的映射索引，实现O(1)路径查询。
3. **节点自包含**：每个节点自包含NDData（多维数据）和TVLStyle（样式），无需外部引用。
4. **维度坐标定位**：结合NDData的维度坐标，实现精确到标点级别的数据定位。

#### 技术方案

```
TDF树 = {
    Root: TDFTreeNode,          // 根节点
    PathIndex: PathIndex,       // 路径索引
    NodeIndex: NodeIndex        // 节点索引
}

TDFTreeNode = {
    ID: string,                 // 节点ID
    ParentID: string,           // 父节点ID
    NodeName: string,           // 节点名称
    NodeType: NodeType,         // 节点类型
    NDData: NDData,             // N维数据
    TVLStyle: TVLStyle,         // 样式
    Children: [TDFTreeNode],    // 子节点
    Metadata: NodeMetadata      // 元数据
}

PathIndex = {
    PathMap: Map<string, TDFTreeNode>,   // 路径→节点
    IndexMap: Map<string, [string]>      // 节点ID→所有路径
}
```

#### 按需增长机制

初始状态：树只有根节点，空间占用 = 根节点大小（约100字节）

添加模块：root → wenshu（增加模块节点）

添加元素：root → wenshu → string_1（增加元素节点）

添加样式：root → wenshu → string_1 → style（增加样式节点）

每个节点只在需要时创建，空间占用最小化。

#### O(1)路径查询

```
查询: get wenshu.string[0].style.font

1. 解析路径: wenshu.string[0].style.font
2. 查找PathIndex.PathMap["wenshu.string[0].style.font"]
3. 直接返回对应节点

时间复杂度: O(1)（哈希查找）
传统遍历: O(n)（需遍历所有节点）
```

#### 维度坐标定位

```
路径: shubiao.table.col[2].row[2]
NDData定位: table[2][2]（第3列第3行）

路径+维度: shubiao.table.col[2].row[2].value
NDData定位: table[2][2][value]（第3列第3行的值）
```

### 有益效果

1. **零空间占用启动**：初始状态只有根节点，按需增长，空间效率极高。
2. **O(1)路径查询**：路径索引实现常数时间查询，查询效率比传统遍历提升5-10倍。
3. **按需增长**：只在需要时创建节点，避免预分配的空间浪费。
4. **节点自包含**：每个节点自包含数据和样式，无需外部引用，数据完整性高。
5. **统一格式**：所有领域数据使用同一树架构存储，消除格式碎片化。
6. **维度坐标融合**：树路径与NDData维度坐标自然融合，位置无歧义。

### 附图说明

图1：TDF树架构示意图
图2：按需增长过程示意图
图3：路径索引查询流程图
图4：维度坐标定位示意图

### 具体实施方式

**实施例1：办公文档存储**

```
初始: root（零空间）

添加文书模块: root → wenshu
添加段落: root → wenshu → string_1
添加样式: root → wenshu → string_1 → style
添加表格: root → wenshu → table_1
添加表格数据: root → wenshu → table_1 → col[0] → row[0] → value
```

**实施例2：印刷数据存储**

```
root → pfd → page[0] → colorplate[0] → element[0] → style
root → pfd → page[0] → colorplate[0] → element[0] → content (NDData)
root → pfd → page[0] → colorplate[0] → element[0] → attribute (CMYK/陷印)
```

**实施例3：动画数据存储**

```
root → animation → object[0] → relation → sine(amplitude=50)
root → animation → object[0] → position → spatial(x=100, y=200)
```

---

## 补充：ND Cell与TD树节点零拷贝绑定（基于`透明统一路径_DD穿透设计方案.md`）

### 补充3.1：零拷贝绑定机制

```
NDChildren:
  TDNode *TDFTreeNode  // 直接引用TD树节点，零拷贝
```

ND Cell不存储JSON文本，存储已解析的TD树节点引用。TDF二进制格式直接编码为树结构，避免二次解析。

### 补充3.2：JSON结构共享模板

```go
type DocumentND struct {
    SchemaTemplate *TDFTreeNode    // 共享结构模板: {user: {name: _, addr: {city: _}}}
    Values         [][]interface{} // 每行文档的叶子值数组 [["张三", "北京"], ["李四", "上海"]]
}
```

- 相同结构的JSON对象共享TD节点模板，仅存储叶子值差异
- 写入时：结构变更→新建模板版本；值变更→修改Values数组
- 读取时：模板+值→重组JSON(零拷贝视图)

### 补充3.3：DD层倒排索引

```go
type DDFieldIndex struct {
    FieldPath  string            // "user.addr.city"
    ValueIndex map[string][]CellRef  // 值 → ND cell坐标列表
}
```

为高频JSON字段建立倒排索引，查询时直接定位ND cell，跳过全表扫描。

### 补充3.4：存储压缩效果

| 优化前 | 优化后 | 压缩比 |
|--------|--------|--------|
| 每文档存储完整JSON | 共享模板+值数组 | 5-20倍 |
| 查询需解析JSON | 模板+值直接重组 | 10-100倍 |
| 部分更新需重写整个文档 | 仅修改Values数组 | 100-1000倍 |

---

## 许可与授权

**版权归属**：所有理论和基于理论体系发明的产品发明专利、著作权归四川龙映科技有限公司陈钦所有。

**授权条款**：本专利文档采用 **GNU AFFERO GENERAL PUBLIC LICENSE v3.0（AGPL-3.0）** 协议开源，并附加以下条款：

1. **引用规范**：任何人无偿使用、引用必须说明出处，包括发明人姓名、单位及原始文档链接。
2. **改进回馈**：改进后请回馈仓库，基于本专利文档的改进、衍生作品或增强版本，必须回馈至本仓库，不得私有化。
3. **非商用无偿**：非盈利用途可无偿使用，包括个人学习、学术研究、科研项目、开源社区贡献等。
4. **商用授权**：商用需获得授权付费使用。商业用途需获得书面授权并支付授权费用。商业用途包括但不限于：将本技术方案用于商业产品、商业服务、商业系统、商业咨询等。
5. **法律保护**：本产品受国际法的保护，侵权必究。
6. **禁止剽窃**：禁止剽窃本专利文档的内容并据为己有，禁止删除或篡改原始版权信息。

**发明人**: 陈钦 (cq800229@qq.com)  
**单位**: 四川龙映科技有限公司  
**申请日期**: 2026-09-14
