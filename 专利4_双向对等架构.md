# 发明专利文档

## 发明名称: 数据描述与操作双向对等架构

### 技术领域

本发明属于软件架构技术领域，具体涉及一种数据描述与操作双向对等的架构方法，实现数据描述层与数据操作层的完全对等和统一。

### 背景技术

传统软件架构中，数据描述（数据长什么样）和数据操作（怎么操作数据）是分离的：

1. **描述与操作分离**：数据用JSON/XML描述，操作用API/SQL描述，两种语言互不兼容。
2. **认知负担重**：开发者需要同时掌握数据描述语言和操作语言，增加学习成本。
3. **一致性难以保证**：数据描述和操作使用不同抽象层级，容易出现不一致。
4. **AI理解困难**：AI需要理解两种不同的语言才能操作数据，增加了复杂度和错误率。
5. **扩展性受限**：新增操作类型需要修改操作语言，新增数据类型需要修改描述语言。

### 发明内容

本发明提出一种数据描述与操作双向对等的架构，实现数据描述层与数据操作层的完全对等。

#### 核心创新

1. **双向对等**：数据描述（NDData）和数据操作（TCL/TQL）是完全对等的——每一个ND数据都有对应的TCL路径，每一个TCL语句都对应ND中的具体数据。
2. **统一抽象**：数据描述和数据操作使用同一套抽象（维度+路径），只是关注点不同（描述关注"是什么"，操作关注"怎么操作"）。
3. **确定性映射**：从TCL路径到ND数据有确定性的映射关系，相同路径+相同操作=相同结果。
4. **正交性**：NDData管"数据是什么"，TCL管"怎样操作数据"，两者职责天然正交，互不干扰。

#### 技术方案

```
双向对等架构:

NDData描述层:
  ┌─────────────────────────────┐
  │  NDData: 数据是什么          │
  │  ├── Dimensions: 维度定义    │
  │  ├── Shape: 数据形状         │
  │  ├── Strides: 内存步长       │
  │  └── Data: 实际数据          │
  └─────────────────────────────┘
           ↕ 完全对等
TCL操作层:
  ┌─────────────────────────────┐
  │  TCL/TQL: 怎样操作数据        │
  │  ├── get: 查询操作           │
  │  ├── set: 修改操作           │
  │  ├── add: 添加操作           │
  │  ├── del: 删除操作           │
  │  └── broadcast: 广播操作     │
  └─────────────────────────────┘

对等关系:
  NDData维度 ←→ TCL路径段
  NDData坐标 ←→ TCL索引
  NDData值   ←→ TCL操作数
```

#### 对等性证明

**每一个ND数据都有对应的TCL路径**：

```
NDData: pfd.page[1].colorplate[0].element[5].attribute.trapping
TCL路径: pfd.page[1].colorplate[0].element[5].attribute.trapping

两者完全一致，只是关注点不同：
- NDData关注: 这个数据在第1页第0色版第5个元素的陷印属性是什么
- TCL关注: 如何获取/设置第1页第0色版第5个元素的陷印属性
```

**每一个TCL语句都对应ND中的具体数据**：

```tcl
set pfd.page[*].colorplate[*].element[*].fill = "#000000"
```

对应NDData操作：
- 维度: page, colorplate, element, attribute
- 坐标: 所有维度的所有元素
- 操作: 设置fill属性为"#000000"
- 结果: 所有页面所有色版所有元素的填充色变为黑色

#### 正交性

```
NDData职责: 数据是什么
  ├── 维度定义（页面、色版、元素、属性）
  ├── 数据形状（各维度大小）
  └── 实际数据值

TCL职责: 怎样操作数据
  ├── 查询（get）
  ├── 修改（set）
  ├── 添加（add）
  ├── 删除（del）
  └── 广播（broadcast）

正交性: NDData变化不影响TCL语法，TCL操作不影响NDData结构
```

### 有益效果

1. **降低认知负担**：一种抽象（维度+路径）同时覆盖描述和操作，开发者只需掌握一套概念。
2. **保证一致性**：数据描述和操作使用同一套抽象，一致性由架构保证。
3. **AI友好**：AI只需理解一种抽象即可操作数据，降低AI理解和出错率。
4. **可预测性**：相同路径+相同操作=相同结果，结果完全可预测。
5. **可组合性**：NDData和TCL可以独立扩展，互不影响。
6. **跨领域通用**：同一套双向对等架构适用于所有领域。

### 附图说明

图1：双向对等架构示意图
图2：NDData与TCL映射关系图
图3：正交性示意图
图4：跨领域应用示例

### 具体实施方式

**实施例1：办公文档**

```
NDData描述: wenshu.string[0].content = "Hello World"
TCL操作: get wenshu.string[0].content
描述和操作使用相同的路径结构
```

**实施例2：印刷数据**

```
NDData描述: pfd.page[0].colorplate[0].element[0].fill = "#FF0000"
TCL操作: set pfd.page[0].colorplate[0].element[0].fill = "#000000"
描述和操作使用相同的路径结构
```

**实施例3：动画数据**

```
NDData描述: objects[0].position.x = 100
TCL操作: set objects[0].position.x = 200
描述和操作使用相同的路径结构
```

---

## 补充：TD+ND+DD三层双向对等（基于`透明统一路径_DD穿透设计方案.md`）

### 补充4.1：三层对等扩展

原始双向对等：NDData ↔ TCL/TQL（数据描述层 ↔ 数据操作层）

扩展后三层对等：

```
TD层 ↔ TD路径导航
  ├── 数据描述: TD树节点定义文档结构
  └── 数据操作: TD路径导航定位文档节点

ND层 ↔ ND坐标定位
  ├── 数据描述: NDData定义多维数据内容
  └── 数据操作: TCL/TQL定位和操作ND坐标

DD层 ↔ DD路径穿透
  ├── 数据描述: ND Cell内嵌JSON文档结构
  └── 数据操作: DD路径穿透导航JSON字段
```

### 补充4.2：统一抽象扩展

```
统一抽象 = TD路径 + ND坐标 + DD路径

TD: 文档/对象/数组的层级定位
ND: 多维数据的坐标定位
DD: JSON内部字段的穿透定位

三者使用统一的路径表达式语法，仅关注点不同：
- TD关注"文档在哪里"
- ND关注"数据是什么"
- DD关注"字段是什么"
```

### 补充4.3：正交性扩展

```
NDData职责: 数据是什么
  ├── TD层: 文档结构定义
  ├── ND层: 多维数据内容
  └── DD层: JSON文档内嵌结构

TCL/TQL职责: 怎样操作数据
  ├── TD操作: 文档导航(add/del/get)
  ├── ND操作: 多维操作(get/set/broadcast/project/filter/pivot)
  └── DD操作: JSON穿透导航(get/set/broadcast)

正交性: 三层职责独立，扩展互不影响
```

---

## 许可与授权

**版权归属**：所有理论和基于理论体系发明的产品发明专利、著作权归四川龙映科技有限公司陈钦所有。

**授权条款**：本专利文档采用 **GNU AFFERO GENERAL PUBLIC LICENSE v3.0（AGPL-3.0）** 协议开源，并附加以下条款：

1. **引用规范**：任何人无偿使用、引用必须说明出处，包括发明人姓名、单位及原始文档链接。
2. **改进回馈**：改进后请回馈仓库，基于本专利文档的改进、衍生作品或增强版本，必须回馈至本仓库，不得私有化。
3. **非商用无偿**：非盈利用途可无偿使用，包括个人学习、学术研究、科研项目、开源社区贡献等。
4. **商用授权**：商用需获得授权付费使用。商业用途需获得书面授权并支付授权费用。商业用途包括但不限于：将本技术方案用于商业产品、商业服务、商业系统、商业咨询等。
5. **法律保护**：本产品受国际法的保护，侵权必究。
6. **禁止剽窃**：禁止剽窃本专利文档的内容并据为己有，禁止删除或篡改原始版权信息。

**发明人**: 陈钦 (cq800229@qq.com)  
**单位**: 四川龙映科技有限公司  
**申请日期**: 2026-09-14
